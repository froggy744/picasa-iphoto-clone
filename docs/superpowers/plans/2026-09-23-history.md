# Phase 1 History implementation plan

Spec: `TODO-HISTORY.md`, Phase 1 only. Execute inline.

Add SQLite event, event-item, recent-item and collage-project tables without changing originals. Use photo IDs for grid identity, including registered collage exports; associate each collage export with its editable JSON project. Record only committed changes, transactionally with recipes. Reuse one event ID across batch chunks. Do not backfill imports as edits.

- [x] Add persistence tests for ordering, repeated edits, no-op saves, batch grouping, rollback and collage project reopening.
- [x] Implement database operations in `src/db/history.rs`; integrate photo Done, reset, paste and rotation commit points.
- [x] Persist collage projects on successful export and reopen the selected project from History.
- [x] Add History sidebar/filter and existing-grid presentation, ordered by last commit with type/date captions and existing cached thumbnails/offline badges.
- [x] Run targeted tests, formatting, cargo check and the full test suite once; review the diff.

Constraints: preserve library data, non-destructive edits, existing destinations and caches. No Phase 2/3 work. Tests must cover batch chunk boundaries and failed writes, and ensure imports and unchanged saves produce no events. GTK interactions require display-based validation where available.

Verification: 355 full-suite tests passed, 21 ignored; seven History/export tests passed; the ignored History GTK test was run explicitly and passed. Cargo check and git diff --check passed. New modules pass rustfmt; repository-wide cargo fmt --check remains red on pre-existing formatting.

Review fixes: real tile binding now refreshes History captions and invalidates changed presentation keys; collage export protects unrelated files and stages/rolls back output on save failure; a modal save operation and transactional draft identity prevent overlapping export/resume identity loss. Changes remain uncommitted on rc5 at the user's request. See DEVELOPMENT_HANDOVER.md for continuation.
