# Agent progress log (power-outage safety) — chunked Folder churn fix

## Task
Eliminate GTK thumbnail rebinding/model churn in experimental chunked Folder view (baseline 243,082 gtk_bind).
Branch: `experiment/folder-gridview`, base `247c4d157bc1d17be9bcf7d6e1bdd0b450fb2fa8`.
User granted autonomous lead mode: implement → review → test → benchmark → commit → push.
User asks: play notification bell on FULL completion; always write progress to file (doing);
resume from checkpoint, do not restart; use rust-analyzer LSP for Rust work (verified installed 1.98.1,
global config `lsp:true`, but no `lsp` tool exposed in this session and no process running — still use targeted reads).

## ROOT CAUSE FOUND (thumbnails stuck as placeholders in chunked mode)
1. `drain_thumbnail_display_completions` (virtualization.rs:1102-1103) only walks `self.root` +
   `self.folder_root`. Chunked tiles live in inner GridViews under `chunked.root` (separate widget
   tree in gallery_scroll_stack) → decoded paintables NEVER applied → placeholder wall.
2. No chunked equivalent of `refresh_visible_grid_tiles`; initial 13,120-submit wave exceeded
   DISPLAY_QUEUE_CAPACITY (1024), submits silently dropped, no rebind retry (churn gone) → stuck.

## Fix implemented (uncommitted)
- virtualization.rs `drain_thumbnail_display_completions`: add `collect_tiles(chunked.root)`.
- virtualization.rs: new `refresh_visible_chunked_tiles()` mirroring refresh_visible_grid_tiles
  against chunked.root (unload+load visible chunked tiles → re-apply RAM cache or re-queue).
- build.rs folder_view_changed_handler chunked branch: schedule refresh_visible_chunked_tiles()
  after 90ms (mirrors gridview/legacy-folder paths at :1547/:1562).
- build.rs: scroll-settle debounce on folder_chunked_scroll vadjustment → refresh chunked tiles
  140ms after scrolling stops (rows stay realized, never rebind → need explicit heal).
- cargo check --offline clean (0 errors, ~136 warnings); cargo build done.

## Completed (before fix, verified)
- [x] Implementer rewrote `src/grid/chunked.rs`: incremental `sync_chunks` (append tail slices at
      i*64, fixed size=64, splice truncate, no-op same count), `grids: Vec<WeakRef<GridView>>`,
      `PIC_CHUNK` counters. Reviewered CORRECT vs GTK gtkslicelistmodel.c.
- [x] Test convention: 6 chunk tests `#[ignore=gtk]` + inline gtk::init; pass individually;
      default 376 pass / 31 ignored; --include-ignored fails pre-existing (gtk::init two-thread).
- [x] Baseline on chunked run: 13,313 gtk_bind vs 243,082 (~94.5% cut), 13,120 distinct, max 2,
      outer setup=205/bind=205/unbind=0, 46 syncs all create, 0 GTK warnings.
- [x] App death diagnosis: shell-tool 120s timeout kills process group → relaunch with `setsid`
      (new session) survives; unrelated to chunked fix.

## In progress
- [ ] Re-run with fix (new binary): verify thumbnails appear on visible tiles, assigns grow,
      still 13,313 binds baseline, 0 warnings.
- [ ] Navigate to bubs preg; scroll; capture final metrics vs 243,082/13,120/33.
- [ ] Commit src/grid/chunked.rs + virtualization.rs + build.rs ("Fix chunked Folder thumbnails:
      include chunked tiles in completion drain and reconcile visible tiles") + push
      (user approved commit+push if test passes). Do NOT commit .agent-progress.md.
- [ ] Report measurements + remaining problems (outer over-realization, store ref cycle,
      store.remove tail rebind, queue-eviction retry gap mostly resolved by scroll-settle).
- [ ] FULL completion → notification bell (printf '\a' / paplay complete.oga).

## Key facts
- Interactive run: setsid bash -c 'GDK_BACKEND=x11 PICASA_FOLDER_CHUNKED=1 PICASA_TRACE=1
  ./target/debug/pic-rs > /tmp/chunkfixN.log 2>&1; echo "APP_EXIT=$?" >> /tmp/chunkfixN.log'
- Screenshot: xwd -id <id> -silent -out /tmp/w.xwd && convert /tmp/w.xwd /tmp/w.png
  (works for the X11 app window only; full GNOME screenshot denied: Shell.Screenshot AccessDenied,
  portal RemoteDesktop/ScreenCast would need dialog click — deferred).
- Input: xdotool windowfocus <id>; mousemove (img_x+395, img_y+179) click 1; click 5 = wheel down.
- Trace: PIC_CHUNK outer/sync, PIC_THUMBNAIL gtk_bind/cache_lookup/paintable_assign,
  PIC_NAV gallery_replace.

## 2026-09-24 Manual test session (user-driven) — NEW findings
- Launched chunkfix5.log (PID 59892, window 10485762) fresh: idle baseline SOLID
  (13,281 binds / 13,120 distinct / 0 warns / 0/s churn) after the drain+refresh fix.
- **User clicked folder buses preg (395 photos)** → reproduced the LIVE bug:
  - gallery_replace oscillates 487 ↔ 22623/22330 across the click (store flip-flop).
  - Each flip tears down ALL 354 chunks (sync_truncate 8) then rebuilds (create 354).
  - Binds for one folder click ~13,866 affecting 13,120 distinct stale ids —
    the ListView window spans the OLD All Photos set, not the 395-photo folder.
  - 1,417 Gtk-WARNING "Finalizing GtkGridView but it still has children left" —
    chunk GridViews finalized mid-teardown.
  - App load slow; user reports "force quit/wait then jumps to other photos".
- Root cause: folder navigation path (layout.rs refresh_grid_inner / destination_click)
  loads ALL photos (`db::photos(None)`, 22,623) for Folder mode — the "continuous
  stream" design loads every indexed folder. bubs preg's 395 photos never arrive
  as a query boundary; the store stays the full library.
- Found competing double-load: build.rs ~880-920 loads `db::photos(Some(folder.id))`
  (487 incl subfolders) via alternate nav path. Two query paths race (487 vs 22623).
- Coupled with chunked prototype bound to the FULL store at construction
  (view.rs:250): folder switch splices the store (487) then immediately splices
  back (22623), so the chunked outer model rebuilds 354 chunks twice+.
- Next: decide whether to (a) restrict Folder-mode query to the selected folder in
  chunked path, or (b) fix the double-load race in build.rs ~880-920.

## 2026-09-24 Chunk-churn root-cause + fix session (todo-qwen3.8-27b.md)
- Confirmed 3 root causes (previous conversation): RC1 single-folder gallery.replace()
  from lightbox collection_navigation (487↔22623 oscillation, ~1,417 finalizing warns);
  RC2 chunked mode scrolls a hidden plain GridView (self.root) instead of visible
  chunked.root; RC3 inner GridViews finalize with realized models on teardown.
- **Verify-only results (pre-fix):** clean idle run (chunkfix10) proved NO periodic
  Section crawl — earlier "crawls" were user cursor clicks (tick frozen at 19, user
  searches reboot the trace); startup replays nav history once per step. grouping.rs
  columns exemption applied (chunkfix11): 9/9 folder_cache_restore result=hit, zero
  column-mismatch rejects.
- **Fix applied (per todo plan):**
  - build.rs collection_navigation: deleted dead SidebarFilter::Folder(_) arm;
    both album→first-folder and library-adjacent-folder arms now do
    can_restore_folder_cache() ? scroll_to_folder : refresh_grid_to_folder (no
    single-folder replace; lightbox keeps per-folder objects).
  - selection.rs: scroll_to_folder/select_photo/select_last_photo route through
    chunked_prototype.scroll_to_photo(position) in chunked Folder mode.
  - chunked.rs: new scroll_to_photo(position) (outer root.scroll_to(chunk)); Step 3
    pre-clear inner grid models before sync_chunks for grids beyond `want`.
- **Live verify (chunkfix12, PICASA_FOLDER_GRIDVIEW=1 PICASA_FOLDER_CHUNKED=1):**
  folder navigations = folder_cache_restore result=hit (Folders 47/29/62, lightbox
  cross Folder(1)↔Folder(24) via click), NO 487 replaces, Finalizing warnings 145→6
  (all 6 during one-time startup nav-history replay, zero during user nav), 5 Right-
  arrow lightbox crossings after launch = 0 new gallery_replace / 0 new Finalizing.
- Full suite: 376 passed / 31 ignored; chunked 6 display tests pass individually.
- Stripped temp traces (sidebar.rs PIC_SIDEBAR/LIST_MUTATION_TICK, library.rs
  PIC_NAV refresh_* restored to HEAD; chunked PIC_CHUNK counters removed). grouping.rs
  indentation CRLF-drift repaired. Build + tests clean.
- NEXT: commit (5 files: chunked, grouping, selection, virtualization, build;
  do NOT commit .agent-progress.md <-- this file), push needs user approval
  (git push pattern-denied), notification bell on full completion.

## 2026-09-24 session (verify + crash fix)
- 87d035d8 committed: 5-file chunked fix (build/grouping/selection/virtualization/chunked),
  verified live (chunkfix12: all hits, zero gallery_replace, Finalizing 145->6, stopwatch OK). 
  Push put on hold by user.
- USER TEST found a crash: "done, it crashed. when i do cargo run it works." chunkfix13.log
  = silent SIGSEGV mid gtk_bind right after folder_cache_restore hit, 0 Finalizing, 0 panics.
  coredump 112588 (22:16:56) backtrace: ChunkedPrototype::new items_changed_trampoline ->
  gtk_grid_view_set_model -> gtk_list_item_manager_remove_items -> SIGSEGV. Re-modeling the
  surplus GridViews (set_model(None)) synchronously inside GTK's base-store items-changed
  emission re-enters the list item manager while mid-bind -> crash.
- FIX (c15966f1): chunked.rs reconcile (pre-clear + sync_chunks) moved to a coalesced
  glib::idle_add_local so emission unwinds first; scroll_to_photo flushes pending reconcile
  so target chunk exists before GTK clamps scroll. split reconcile_chunks out; store/chunks/
  pending flags kept on ChunkedPrototype. Build clean, 376 pass, 6 chunked tests pass.
- NEXT: user re-tests "bubs preg"-style folder (large ~400) + lightbox Down across folder
  boundary on the fixed binary (with the 3 env vars). If clean, push approval + bell.

## 2026-09-24 session 2 (chunkfix15/16/17/18 regression: hit+replace on every nav)
- chunkfix15 (user re-test of c15966f1): NO crash, 0 Finalizing (crash fix held), but
  "serious issues": every folder nav = folder_cache_restore result=hit IMMEDIATELY followed
  by gallery_replace photos=N (folder-scoped counts), 0 rejects ever, chunk churn 354<->1<->354,
  slow replaces 722ms(22623)/497ms(28)/204ms etc.
- ROOT CAUSE (proven via PICASA_TRACE_BACKTRACE backtraces, /tmp/chunkfix17.log):
  1) GTK ListBox row_selected fires PROGRAMMATICALLY at startup (sidebar build/restore walk:
     History -> Album(1) -> Album(2) -> Album(4)) -> on_filter -> destination_click ->
     can_restore_folder_cache "hit" (cache exists after any folder rebuild, never invalidated,
     columns check skipped for chunked exp) -> scroll_to_folder fails silently ->
     fall-through refresh_grid_to_folder -> full replace. The "folder" replaces in old logs
     were actually History/Album replaces.
  2) can_restore_folder_cache answers "a cache exists", callers use it as "current model IS
     the folder stream and contains target folder". Not true after album/search/history replace.
  3) IndexingFinished -> refresh_grid unconditionally -> one full 22623 replace PER scan job.
  4) build.rs nav arms ignored scroll_to_folder's false return (hit -> nothing loads!).
- FIX (this commit):
  * grouping.rs: try_restore_folder_navigation(id, path) = mode==Folder AND cache valid AND
    scroll_to_folder succeeded (atomic). Traces: reject reason=not_folder_mode /
    target_folder_missing_from_stream.
  * build.rs both folder nav arms + layout.rs destination_click use it; layout only evaluates
    restore for folder targets (albums/history no longer log bogus hits); exact_photo_target
    keeps old non-atomic check (scrolls itself).
  * sidebar.rs: NAVIGATION_ENABLED AtomicBool gate on on_filter; suppressed until startup
    restore completes (set in restore_startup_view + 5s fallback timer) -> kills the startup
    selection cascade entirely.
  * build.rs IndexingFinished: coalesced 750ms debounce -> ONE grid rebuild per scan burst.
  * grid.rs: regression test cache_hit_folder_navigation_never_replaces_the_model (asserts
    replace_generation unchanged across hit-scroll, stale-mode reject, missing-target reject).
- VERIFY (chunkfix18, driven with xdotool): startup quiet (2 replaces only, was ~12);
  first folder entry = 1 replace 53ms (was 722ms); folder->folder = hit + ZERO replaces;
  History->folder = reject(not_folder_mode) + 1 correct reload 46ms; cross-view replaces
  correct; zero chunk churn after initial build. 376 unit tests + all 6 chunked tests pass;
  new regression test passes. PID of test instance may still be running.
- NEXT: push, then deployment-readiness agents (Flatpak/Play/macOS), RC2/RC3 residual
  (cross-view replace cost 673ms teardown of 22623-photo model is real work, not a bug).

## 2026-09-25 session (agents + perf + crash recovery)
- e6a89926: hit+replace regression fix (try_restore_folder_navigation atomic check, sidebar
  NAVIGATION_ENABLED startup gate, IndexingFinished 750ms debounce, regression test
  cache_hit_folder_navigation_never_replaces_the_model). Verified chunkfix18 via xdotool:
  startup quiet (2 replaces vs ~12), folder->folder = hit + ZERO replaces, 53ms first folder
  load (was 722ms), History->folder = reject(not_folder_mode) + one correct 46ms reload.
- d95a25b2: deployment readiness. AGENT 1 created packaging/flatpak/: io.github.froggy744.PIC
  .desktop (desktop-file-validate PASS), .metainfo.xml (appstreamcli validate 0 errors),
  .json manifest, pic-rs-launcher, FLATHUB.md; build scripts install committed files.
  I renamed app id io.github.you.PicasaRs -> io.github.froggy744.PIC (main.rs + scripts),
  added Cargo [package] authors/repository/homepage/readme/keywords/categories +
  [profile.release] strip="symbols" (release binary 20.9MB, runs OK), debug log moved from
  /tmp/pic-ui-debug.log to dirs::cache_dir()/pic-rs/ui-debug.log.
- AGENT 2 audit verdicts: Google Play/Android NOT feasible (GTK4 no Android backend; native
  C probes; dirs=None; 6+ mo rewrite) - ship existing Flatpak to Chromebook/Crostini instead.
  macOS feasible 2-4 wks (codebase already macOS-shaped; needs gtk-macOS-bundler + notarization).
  keyring "blocker" was FALSE ALARM: src/smb_transport.rs is dead code (no `mod smb_transport`
  in main.rs - 1639-line design doc, never compiled). smb Transport wiring is future work.
- a4347b1a: perf. ChunkedPrototype::detach_for_replace/reattach_after_replace: unmount outer
  chunk rows before wholesale shrink splices; 22623->50 transition 673ms->53ms debug,
  706ms->58ms release (12x). Startup batch loop now aborts on replace_generation change
  (new pub fn Gallery::replace_generation()); stale batches no longer splice into a
  superseded view (fixes hybrid chunk counts + first-transition 702ms pollution).
- FLATHUB REMAINING (needs user/repo decisions): hosted screenshots (uncomment block in
  metainfo, raw.githubusercontent URLs), manifest source type:git, flatpak-cargo-generator
  vendoring, release notes cadence, permissions justification (filesystem=host, network).
- NOT PUSHED (git push denied for me): e6a89926 d95a25b2 a4347b1a - USER MUST PUSH.
  User's own uncommitted edit: themes/ww/theme.css (left alone).
- NEXT: re-verify live after crash (instance killed), consider first-transition race
  (506ms when nav lands mid-startup-build), then Windows script parity check for renamed
  app id (build.windows.ps1 may still reference old id).

## 2026-09-25 session 2 (user: "very slow with trace logs")
- User logs: fix22 (traced) 672ms Favorites replace was TRACE amplification (126k-157k
  eprintln lines/run, var_os lookup PER line); fix24 (untraced) reports own
  cold_start_ms=116 - app itself is FAST.
- FIX commit: diagnostics::trace_enabled()/tile_trace_enabled() (OnceLock, read env once);
  45 call sites migrated; per-tile lines (gtk_bind, paintable_assign, cache_lookup,
  queue_wait) now require PICASA_TRACE_TILES. Traced session log: 170k -> ~500 lines.
- Also: deferred drop of old PhotoObjects (>1000) to an idle in Gallery::replace small path;
  splice now only decrefs refs, GObject finalizers (textures) run off the nav frame.
  First cross-view transition (racing startup build) 674ms -> 211ms release.
- Verified: 376 tests pass, folder nav scroll-only, 0 Finalizing, release + debug build.
- NEXT: user manual re-test with the same env (trace now cheap); push 5 commits.

## STATUS 2026-09-25 07:00 (latest)
- Branch experiment/folder-gridview, 5 commits ahead of origin (origin at c15966f1):
  e6a89926 atomic folder-cache restore + startup nav gate + scan debounce + regression test
  d95a25b2 deployment readiness (app id io.github.froggy744.PIC, Cargo metadata, strip,
            packaging/flatpak: desktop+metainfo validated, manifest, launcher, FLATHUB.md)
  a4347b1a chunk detach before shrink-splices (673->53ms cross-view) + startup batch guard
  745c6acb user's own theme edits ("Experimental Fixes")
  d2a402e1 cheap PICASA_TRACE (env cached once, per-tile spam behind PICASA_TRACE_TILES,
            170k->500 lines/session) + deferred old-photo finalizers (first transition 674->211ms)
- Verified: 376 unit tests + chunked tests pass; live xdotool runs: folder nav = cache-hit
  scroll-only (zero replaces), 0 Finalizing, release binary runs clean (20.9MB stripped).
- Agents done: Flatpak packaging files created+validated; Play=not feasible (use Flatpak on
  Chromebook/Crostini), macOS feasible 2-4wks, smb_transport.rs = dead code (never compiled).
- Flathub TODO (user decisions): hosted screenshots, confirm app id, manifest type:git,
  cargo vendoring, release notes cadence.
- USER: manual run command = GDK_BACKEND=x11 PICASA_FOLDER_GRIDVIEW=1 PICASA_FOLDER_CHUNKED=1
  PICASA_TRACE=1 ./target/debug/pic-rs > /tmp/chunkfixN.log 2>&1  (trace now cheap).
  PUSH IS MANUAL (git push denied for opencode).

## 2026-09-25 07:15 (fix28/fix29 analysis + follow-ups)
- fix28 (traced, 1546 lines - trace fix works): cold_start 107ms. User was ACTIVELY clicking:
  first album click after full stream = 575ms (races startup build; later album clicks
  52/123/36ms OK). Startup nav sequence was 0 -> 50(RecentlyAdded) -> 28(Favorites) ->
  22624(stream); the Favorites nav did NOT reproduce in 2 instrumented runs (bt28.log) -
  parked, benign (32ms). Seven replace(22624) were all <20ms unchanged fast paths - fine.
- fix29 (user ran with ONLY PICASA_TRACE_TILES=1 -> tile lines only, no nav lines; BOTH env
  vars needed for full analysis). Tiles healthy: max gtk_bind 1.1ms, paintable <=62us,
  NFS cache misses 5.5k but 7-68us each. No tile-level smoking gun.
- 1cf368c3: deferred disposal now slices 4000 objects per idle (one 20k finalization pass
  was a single long frame right after the nav paint).
- All 376 tests + chunked tests pass; release rebuilt. USER AWAY AT WORK - will check later.
- NEXT when user returns: ask them to re-test with BOTH PICASA_TRACE=1 and
  PICASA_TRACE_TILES=1 if we need full traces; push still manual (7 commits ahead now:
  e6a89926 d95a25b2 a4347b1a 745c6acb d2a402e1 6f6a7dba 1cf368c3).

## 2026-09-25 FIRST-CLICK INVESTIGATION (e0addb01 + a168f9c0 instrumentation)
- Method: per-stage nav tracing (PICASA_TRACE), chunk factory setup/bind/unbind counters,
  adjustment values in bind trace, gdb sampling attempts (too slow - attach misses 600ms),
  control run without chunked prototype. Repro: load folder 52 stream (22624), first
  cross-view click (Album(6)), debug build, xdotool-driven.
- TIMING TABLE (first click, old_items=22624 -> new_items=6):
  sidebar click -> destination_click ........ ~0 ms
  stack switch (photos page) ................ 2-71 ms
  grouping applied (set_grouping) ........... 2-78 ms
  DB open / query / sort+retain ............. 26 / 76 / 40 ms (background thread)
  spawn -> result latency ................... 150 ms (25ms poll x6, overlapped)
  replace: detach_for_replace ............... 540-600 ms  <<< DOMINANT OWNER
  replace: PhotoObject creation (6) ......... 0.07 ms
  replace: model vec swap ................... ~0
  replace: base store splice (22624 out) .... 13.6-15.3 ms
  replace: selection select_item ............ 0.4-3.6 ms
  replace: group model work ................. 0
  replace total ............................. 556-618 ms
  dispose slices (6 idles x 4000) ........... ~0.8 ms total
  app idle after replace .................... 4-5 ms
- CONTROL: identical transition WITHOUT chunked prototype = 11.9 ms TOTAL (splice 11.4).
  Chunked prototype is the sole owner (50x).
- CAUSE CHAIN (measured): during EVERY folder-stream fill every new chunk row
  materializes+binds immediately (~31ms/frame, binds 1:1 with chunk creations, stops when
  fill stops), NEVER unbinds (0 unbinds per session). ListView adjustment reports
  upper=page=872 (degenerate: inner GridView row heights unknown -> whole list believed
  one viewport) => NO virtualization => 205-354 chunk GridViews + ~13k tiles stay mounted.
  Cross-view nav then destroys them synchronously: set_model(None)=540-600ms, or the same
  cost moves into the splice if detach is deferred (navtrace4: splice 622ms, deferred
  detach 4us - cost is intrinsic to mounted-row teardown, not the call site).
- FAILED EXPERIMENT (reverted): set_height_request on inner grids (final chunk height
  13*(tile+12)) - bind count unchanged (205 in all runs), upper still 872. GtkListBase row
  height tracking does not honor child minimum-height for its adjustment in this nested
  GridView-in-ListView setup.
- Note: with only PICASA_TRACE_TILES=1 there are NO nav lines (fix29) - both env vars
  needed. Frames stop when window loses focus -> materialization pauses (frame-paced).
- PROPOSED FIX (next, needs go-ahead): viewport-windowed chunk realization - sync_chunks
  creates slices near the viewport (+margin) only; extend on scroll/value-changed; keeps
  mounted rows bounded (~10-15) so fill churn AND teardown both collapse. Alternative:
  solve nested measurement so upper reflects real heights (harder, GTK internals).
- Commit a168f9c0 = instrumentation only; height experiment REVERTED (working tree clean
  except user's themes/ww/theme.css).

## FIRST-CLICK REPORT (final, user-facing summary)
Timing table for the first click (22624 -> 6, debug build):

| Stage | Time |
|---|---|
| click -> destination_click | ~0 ms |
| stack switch | 2-71 ms |
| grouping applied | 2-78 ms |
| DB open / query / sort (background) | 26 / 76 / 40 ms |
| spawn -> result (6 x 25ms polls) | 150 ms (overlapped) |
| replace: detach_for_replace | 540-600 ms  <-- DOMINANT |
| replace: 6x PhotoObject creation | 0.07 ms |
| replace: base-store splice (22624 out) | 13.6-15.3 ms |
| replace: selection | 0.4-3.6 ms |
| replace: group work / disposal slices / app idle | 0 / 0.8 / 4-5 ms |
| Total replace | 556-618 ms |

CONTROL: identical transition without the chunked prototype = 11.9 ms total -
the chunked prototype is the sole owner (50x).

CAUSE CHAIN (all measured): during every folder fill, each new chunk row
materializes+binds immediately (~31ms/frame) and NEVER unbinds (0 unbinds per
session) - the ListView adjustment reads upper=page=872 (degenerate: nested
GridView row heights unknown -> whole list believed to be one viewport -> no
virtualization). So 205-354 chunk GridViews + ~13k tiles stay mounted, and any
cross-view nav destroys them synchronously inside one frame. Deferring detach
just moves the 600ms into the splice - the teardown is intrinsic. A
height_request experiment did NOT change row measurement (205 binds in every
run) - reverted; only instrumentation is committed (e0addb01, a168f9c0,
docs 0bdcd424).

PROPOSED FIX (awaits go-ahead): viewport-windowed chunk realization -
sync_chunks creates slices only near the viewport (+margin), extended on
scroll. Mounted rows drop to ~10-15, collapsing both the fill churn and the
teardown. Alternative (deeper): fix the nested measurement so upper reflects
real row heights.

## 2026-09-25 SESSION: VIEWPORT-WINDOWED CHUNK REALIZATION (TODO-Final-Experiment.md)
- timestamp: $(date -Is) | branch: experiment/folder-gridview | HEAD: 0bdcd424
- objective: implement viewport-windowed chunk realization per TODO-Final-Experiment.md;
  bounded mounted GridViews, correct scroll geometry, all navigation paths preserved.
- plan: (1) decisive experiment - wrapper Box with per-chunk height_request around inner
  GridView to fix row measurement (upper=872 degenerate state), model set in bind /
  cleared in unbind so GTK's own recycler bounds mounted tiles. (2) remove detach/reattach
  from replace (teardown becomes cheap when only ~10 grids have models). (3) live tests A-H.
- confirmed facts carried in: detach 540-600ms owner; upper=page=872 degenerate; 0 unbinds;
  control 11.9ms; height_request-on-grid experiment alone did NOT fix measurement.
- NEXT: implement wrapper + bind/unbind model management in chunked.rs.
## 2026-09-25 IMPLEMENTATION: viewport-windowed realization
- design: wrapper Box per chunk row (full-chunk height request, partial last chunk
  corrected at bind); bind mounts photo models ONLY when fill complete AND chunk within
  REALIZATION_OVERSCAN_CHUNKS=4 of viewport center; unbind clears model; on fill completion
  (stream_building true->false via new Gallery::set_stream_building / free
  set_stream_building_values for the progressive move closure) chunked.notify_stream_building
  remounts the outer model once -> all rows re-measure against complete slices -> upper spans
  real library -> virtualization works.
- root mechanism proven in /tmp/opencode/list_probe minimal repro: upper =
  rows x average-known-height; progressive fill poisons average (rows materialize while
  slices partial); post-fill model remount fixes measurement. GTK pre-rolls ~205 rows
  (fixed item budget, height-independent) - so rows must be CHEAP when model-less (empty
  wrapper+grid = trivial) while TILES stay bounded by the window.
- cargo build OK; 376 tests + chunked tests pass. Files: chunked.rs, view.rs,
  virtualization.rs.
- NEXT: live TEST A-H matrix with xdotool.

## 2026-09-25 ~10:30 CHECKPOINT — viewport-windowed implementation in progress
- branch experiment/folder-gridview | HEAD e6c1a3fe + UNCOMMITTED rework in
  chunked.rs/view.rs/virtualization.rs | working tree also has user's themes/ww/theme.css (untouched).
- IMPLEMENTED (build OK, 376 tests pass):
  1. Wrapper Box per chunk row, height_request = full-chunk height (partial last chunk
     corrected at bind) -> GtkListBase row measurement no longer degenerate.
  2. Bind gates photo models: none while filling; post-fill only chunks within
     REALIZATION_OVERSCAN_CHUNKS=4 of viewport center; unbind clears model.
  3. Post-fill remount: notify_stream_building(false) schedules (300ms deferred, every fill
     signal re-schedules) a one-shot set_model(None)->Some so all rows re-measure against
     COMPLETE slices. append_photos now sets building true at entry/false at end (was
     missing -> remount never fired; fixed).
  4. Self-heal in the 2s status timer: if fill_complete && upper < expected/2 (progressive
     measurement race), re-run remount, max 5 attempts. Status line now logs expected upper.
- RESULTS: upper now 932k-959k px (was 872!) in most runs; recycling works (unbinds>0);
  cache-hit folder nav = ZERO gallery.replace (TEST C partially verified).
- OPEN PROBLEM: photo area renders BLANK in unattended runs; wrapper allocated_h=0 for all
  rows; value stays 0; clicks produce NO sidebar_select/destination_click traces (clicks not
  reaching the app OR gate suppressing?); 5km row shows selected but no navigation fired.
  Suspicion: window focus/frame-clock issue in xdotool-driven runs, or sidebar click coords
  off; the blank view ALSO occurs on HEAD (pre-rework) so not introduced by rework.
- NEXT ACTIONS: (1) verify click pipeline: grep sidebar_select after clicks, test
  navigation_enabled state; (2) if clicks are dead, drive via keyboard (Down key on grid) or
  start app with last-view already on a folder and screenshot; (3) once rendering confirmed,
  run TEST A-H matrix; (4) commit rework + cleanup bisect env switch (PIC_BISECT_NO_CSS).
- do NOT commit .agent-progress.md (per TODO-Final-Experiment.md).
